language: none

build:
    pre_ci_boot:
        image_name: my_registry_repo/my_image
        image_tag: latest
        pull: true
        options: "-e HOME=/root"

    ci:
        - cd server/
        - sbt compile
        - sbt test
        - cd ..
        - cd extension
        - npm test