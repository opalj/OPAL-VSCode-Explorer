build:
    pre_ci_boot:
    image_name: agssl123/vscodeextension
    image_tag: test
    pull: true
    env: FOO=bar
    options: "-e HOME=/root"
    ci:
        - cd server/
        - sbt compile
        - sbt test
        - cd ..
        - cd extension
        - npm test